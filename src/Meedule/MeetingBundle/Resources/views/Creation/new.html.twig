{% extends 'MeeduleDesignBundle::layout.html.twig' %}

{% block content %}
    <ul class="breadcrumb">
        <li class="active">Crea la riunione <span class="divider">&gt;</span></li>
        <li>Inserisci gli argomenti <span class="divider">&gt;</span></li>
        <li>Condividi</li>
    </ul>

    <h1>Crea la tua riunione</h1>
    <p>Eventi, assemblee o appuntamenti sono momenti importanti ma soprattutto devono essere <b>efficaci</b>.</p>
    <hr />
    
    <form action="{{ path('meeting_create') }}" method="post" {{ form_enctype(form) }} class="form-horizontal">
        {{ form_widget(form) }}
        
        <div class="form-actions">
            <button type="submit" class="btn btn-primary btn-large">Avanti</button>
            o <a href="{{ path('index') }}">
                indietro
            </a>
         </div>
    </form>

{% endblock %}