{% extends 'MeeduleMeetingBundle:Meeting:show.html.twig' %}

{% block commands %}
    <a href="{{ path('meeting_admin_edit', {'id': entity.id }) }}" class="btn">Modifica <i class="icon-wrench"></i></a>
{% endblock %}
    
{% block commands_topics %}
    <div class="pull-right">
        <a href="{{ path('meeting_admin_agenda', {'id': entity.id }) }}" class="btn">Cambia argomenti <i class="icon-wrench"></i></a>
    </div>
{% endblock %}